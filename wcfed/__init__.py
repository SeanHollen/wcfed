"""wcfed — federation between independently-run Claude-agent buses.

Stdlib only, on purpose: the two sides of a federation are run by different
people on different machines, and "pip install" is a negotiation.
"""

__version__ = "0.1.0"
